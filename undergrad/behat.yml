imports:
  - ../includes/extensions/drupal.extension.yml
  - ../includes/extensions/mink.extension.yml
  - behat.local.yml

## Default Profile Settings (Sites)
default:
  paths:
    features: 'features'
  extensions:
    Behat\MinkExtension:
      base_url: https://undergrad.stanford.edu/
    Drupal\DrupalExtension:
      drush:
        alias: sse.ds_undergrad
